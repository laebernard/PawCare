# CORS — dev profile (Spring Boot activates this with -Dspring.profiles.active=dev
# or SPRING_PROFILES_ACTIVE=dev). The default in application.properties already
# covers the local Angular dev server, so this file is left empty intentionally.
