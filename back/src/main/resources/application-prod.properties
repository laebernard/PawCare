# Production profile
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# CORS — set this to your real frontend domain(s)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://pawcare.example.com}
